// AUTO-GENERATED — do not edit. Regenerated by `cargo test`.
// Per-field help via `#[derive(TsParamHelp)]` in `higgs-macros`.

export const LlamaCppParamsHelp = {
    "ctx_len": "How many tokens of conversation the model can hold at once — prompt, chat history, and the reply all share this window. Larger windows let long chats and big documents fit without truncation, but the KV cache grows with it, costing more RAM/VRAM and slower prompt processing; Auto uses the model's trained maximum.",
    "gpu_layers": "How many of the model's transformer layers run on the GPU instead of the CPU; All puts the entire model in VRAM for the fastest generation. Lower the count when the model doesn't fit in VRAM — each layer moved to the CPU frees GPU memory but slows inference, since CPU layers are much slower per token.",
    "threads": "Number of CPU threads used while generating tokens (and for prompt processing unless a separate batch-thread count is set). More threads speed up the CPU-bound parts up to roughly your physical core count; going beyond that adds scheduling overhead and can compete with other applications for CPU time.",
    "use_mmap": "Maps the GGUF file directly from disk instead of copying it into process memory, so loads are near-instant and the OS shares pages between processes. Disabling it forces a full read into RAM, which slows loading but avoids page-fault stalls on slow disks during the first pass through the weights.",
    "use_mlock": "Pins the model's memory pages so the OS can never swap them out to disk, keeping generation latency steady under memory pressure. Only enable it when there's comfortably enough free RAM — locked pages are unavailable to every other application and can starve the rest of the system.",
    "cpu_moe": "For Mixture-of-Experts models, keeps the large expert tensors in system RAM while the attention layers stay on the GPU. This lets an MoE model far bigger than your VRAM still run GPU-accelerated, at the cost of slower expert lookups over the CPU-GPU bus; it does nothing for dense (non-MoE) models.",
    "cpu_buft_overrides": "Advanced: regex patterns matching tensor names that should be kept in CPU memory rather than offloaded to the GPU. Useful for hand-tuning which weights occupy scarce VRAM (e.g. pinning select expert tensors), but a wrong pattern silently moves hot tensors to the CPU and degrades speed.",
    "split_mode": "How the model is divided across multiple GPUs: by layer (each GPU owns whole layers) or by row (tensors are sharded across GPUs). Layer splitting is simpler and usually faster for generation; row splitting can balance very large models better but adds inter-GPU traffic. Irrelevant with a single GPU.",
    "main_gpu": "Which GPU hosts the scratch buffers and small tensors when the model is not being split across devices. Change it to steer the load onto a specific card in a multi-GPU machine (e.g. keep the display GPU free); on single-GPU systems index 0 is the only valid choice.",
    "devices": "Restricts the load to a specific set of GPUs instead of letting the engine use every visible device. Useful to reserve a card for other work or to avoid a slow GPU dragging down a multi-GPU split; listing a single device effectively pins the whole model to it.",
    "n_batch": "Maximum number of prompt tokens submitted to the engine in one logical batch during prefill. Larger batches process long prompts faster by amortizing per-call overhead, but need bigger compute buffers; the default covers the whole context in one pass.",
    "n_ubatch": "The number of tokens actually computed simultaneously in one kernel launch — the logical batch is chopped into chunks of this size. Raising it improves prompt-processing throughput on strong GPUs; lowering it shrinks the compute-buffer memory footprint when VRAM is tight.",
    "n_seq_max": "How many independent sequences the context can serve at once (each gets its own slice of KV cache). Above 1 enables serving concurrent requests from one loaded model, but every extra slot multiplies KV-cache memory; leave at 1 for a single-user chat.",
    "n_threads_batch": "CPU threads used only for the batch/prefill phase, separate from the generation-thread count. Prefill parallelizes better than generation, so it can profitably use more threads; unset, it simply reuses the generation thread count.",
    "flash_attn": "Whether to use the fused flash-attention kernel, which computes attention with far less memory traffic — typically faster and lighter on VRAM for long contexts. Forcing it on fails on backends without the kernel, and quantized KV-cache types require it; Auto lets the engine decide per device.",
    "offload_kqv": "Keeps the KV cache and the attention (KQV) computation on the GPU rather than in system RAM. On = faster attention at the cost of VRAM that grows with context length; off frees that VRAM for model layers but every generated token pays a slow CPU attention pass.",
    "swa_full": "For models trained with sliding-window attention (e.g. Gemma), allocates the full KV cache instead of only the window. This enables correct cache reuse for prompts longer than the window (faster re-prompting), but costs the same KV memory as a non-windowed model of equal context.",
    "type_k": "Data type used to store the attention keys, normally f16. Quantized types (q8_0, q4_0) shrink the KV cache substantially — letting much longer contexts fit in memory — at a small accuracy cost, and they require flash attention to be available.",
    "type_v": "Data type used to store the attention values, normally f16. Quantizing values compounds the memory savings of a quantized key cache for long contexts, but is the more accuracy-sensitive half — prefer quantizing keys first, and note quantized types require flash attention.",
    "rope_scaling_type": "Strategy (linear or YaRN) for stretching the model's rotary position embeddings so it can attend beyond its trained context length. Needed only when forcing a context larger than the model was trained for; the wrong mode for a given model degrades long-range coherence.",
    "rope_freq_base": "Overrides the base frequency of the rotary position embedding, the constant that sets how position information rotates through the dimensions. Some fine-tunes ship a non-standard base to reach longer contexts; setting it incorrectly scrambles positional understanding, so only override when the model card says to.",
    "rope_freq_scale": "Linear scale factor applied to RoPE positions — 0.5 makes the model treat a 2x-trained-length context as if it were in range. A quick way to extend context without YaRN, but coherence degrades as the factor shrinks; leave unset to respect the trained length.",
    "kv_overrides": "Advanced: key=value overrides applied on top of the GGUF file's own metadata at load time, e.g. to correct a wrong architecture field or force an experimental setting. These change how the engine interprets the model, so a bad override can make a working model fail to load or produce garbage.",
    "seed": "Fixes the random seed used by the samplers for every request served by this load, making generations reproducible for testing and comparisons. Unset, each request draws a fresh seed for natural variety; a per-request seed in the sampling parameters still overrides this value.",
} as const;

export type LlamaCppParamsHelpKey = keyof typeof LlamaCppParamsHelp;
